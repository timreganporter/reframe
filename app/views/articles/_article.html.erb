<li>
  <%= link_to article.headline, article.url %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", article, method: :delete,
                                  data: { confirm: "You sure?" } %>
  <% end %>
</li>